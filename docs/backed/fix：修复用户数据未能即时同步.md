# 问题

用户修改个人信息，从数据库同步更新本地缓存后，部分模块需要实现实时更新

- 主页帖子的用户名，头像
- 帖子评论区的用户名与头像

# 思路

- **对于写入**，pinia的store是全局响应式对象，会自动更新。所以在store中引入数据后，不久更新本地缓存，同时调用store中的action来更新状态
- **对于读取**，使用computed来实时计算

# 主页帖子

修改数据来源，由只使用props.post（来源于父组件（帖子列表）从后端拉取的数据）改为如果是当前用户，则使用userstore否则则使用props
